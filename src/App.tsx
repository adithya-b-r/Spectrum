import Button from '@mui/material/Button';

const App = () => {
  return (
    <div>
      <h1>Hello, MUI!</h1>
      <Button variant="contained" color="secondary">
        Click Me
      </Button>
    </div>
  );
};

export default App;